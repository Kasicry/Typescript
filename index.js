// 1. 
// 2. Typescript의 에러메세지 에러메시지의 질이 좋음.
var 이름 = ['kim', 'park'];
이름 = ['kimd'];
var 이름2 = { name: 'kim' };
var 이름3 = 123;
function 함수(x) {
    return x * 2;
}
함수(123);
var john = { name: 'kim' };
var User = /** @class */ (function () {
    function User(name) {
        this.name = name;
    }
    return User;
}());
